# Copy thành .env và điền key
OPENAI_API_KEY=sk-...
CHROMA_DB_PATH=./data/chroma_db
COLLECTION_NAME=etsy_knowledge_base
# Database connection (PostgreSQL or SQLite)
DATABASE_URL=postgresql://etsy_user:etsy_password@localhost:5432/etsy_rag
# Optional: set TESTING=1 during unit tests to skip external clients
TESTING=0